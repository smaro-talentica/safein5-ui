# Production environment
# Production environment
# Temporary: api.safein5.com has no DNS yet — point at the live EC2 backend.
# Production — same-origin /api (Netlify proxies to EC2; avoids mixed content).
VITE_APP_ENV=production
VITE_API_BASE_URL=/api/v1
VITE_STT_ENDPOINT_URL=/api/v1/stt
